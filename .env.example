# Copy this file to .env and fill in real values.
# .env is already gitignored — never commit real keys.

# --- Twilio (telephony) ---
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=

# --- ElevenLabs (text-to-speech) ---
ELEVENLABS_API_KEY=

# --- Deepgram (streaming speech-to-text) ---
DEEPGRAM_API_KEY=

# --- LLM (pick one — only one is needed) ---
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# --- Fixed test target — the ONLY number this bot should ever call ---
TEST_TARGET_NUMBER=+18054398008
